/*
    One Thing Jo hume sabse pehle chahiye for all is to find 
    the width and height 
    and then coordinates ko normalize krna padega

    1. For Line 
      - min(x1,x2,cp.x) --> (0,0)
      - x1 = x1 - min
      - x2 = x2 - min
      - cp = cp.x - min

      Sly for Y
*/
export function convertElementToSvg(element: Element) {}
